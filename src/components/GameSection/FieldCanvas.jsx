import React from 'react';

export default function FieldCanvas() {
    return (
        <div className='field-canvas'>
            {/* We'll render hexes, totems, etc. here */}
        </div> /* This div currently renders 1920 x 1920px circular game field via field.css */
    );
}